{
  "name": "eudi-agent-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Let AI agents request EUDIW / OID4VP credential presentations (age verification and more) via MCP.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "typecheck": "pnpm -r typecheck",
    "dev:server": "pnpm --filter @verifiables/eudi-mcp-example-server dev"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
